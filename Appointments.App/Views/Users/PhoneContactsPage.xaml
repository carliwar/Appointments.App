<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:model="clr-namespace:Appointments.App.ViewModels"
             xmlns:xct="http://xamarin.com/schemas/2020/toolkit"
             x:Class="Appointments.App.Views.Users.PhoneContactsPage">
    <ContentPage.BindingContext>
        <model:PhoneContactsViewModel />
    </ContentPage.BindingContext>

    <StackLayout>
        <Entry x:Name="SearchUser"
           PlaceholderColor="#5084ad"
           Placeholder="Teléfono ó Nombre ó Apellido"
           ReturnCommand="{Binding SearchUserCommand}"
           ReturnCommandParameter="{Binding Text, Source={x:Reference SearchUser}}">
            <Entry.Behaviors>
                <xct:EventToCommandBehavior
                        EventName="TextChanged"
                        Command="{Binding SearchUserCommand}"
                        />
            </Entry.Behaviors>
        </Entry>
        <ListView x:Name="UsersListView"
        ItemsSource="{Binding Users}"
        CachingStrategy="RecycleElement">
            <!--Built in Cells-->
            <ListView.ItemTemplate>
                <DataTemplate>
                    <ViewCell>
                        <Grid>
                            <Button
                            CornerRadius="5"
                            HeightRequest="5"
                            Margin="2"
                            BackgroundColor="{StaticResource Tertiary}"
                            TextColor="{StaticResource BlackText}"
                            Text="{Binding UserFullName}" />
                        </Grid>
                    </ViewCell>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>        
    </StackLayout>
</ContentPage>